#!/bin/sh
# Force native Wayland (Ozone) so Spotify is not blurry on fractional scaling.
exec /usr/share/spotify/spotify \
  --enable-features=UseOzonePlatform,WaylandWindowDecorations \
  --ozone-platform=wayland \
  "$@"
